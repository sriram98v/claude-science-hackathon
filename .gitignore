__pycache__/
*.pyc
.ipynb_checkpoints/
.jupyter_cfg/
.venv/
# Raw dataset (download via ./download_data.sh). No trailing slash so a
# symlink to an external copy is ignored too, not just a real directory.
influenza-hi-antigenic-distance
